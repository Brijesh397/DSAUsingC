#include <stdio.h>
#include <stdint.h>

uint32_t nthPowerOfN(uint32_t no,uint32_t pow);

int main()
{

}


uint32_t nthPowerOfN(uint32_t no,uint32_t pow)
{
	int result;

	if(pow == 1)
	{
		return no;
	}

	result = nthPowerOfN(no, pow-1);

	return result;
}
